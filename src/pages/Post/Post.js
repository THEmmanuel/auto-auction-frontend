import React from 'react';
import style from './Post.module.css';
import Input from '../../components/Input/Input';

const Post = props => {
	return (
		<section>
			Create an auction
			<Input/>
		</section>
	)
}

export default Post;